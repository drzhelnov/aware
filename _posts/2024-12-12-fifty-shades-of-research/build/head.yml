---
include: false
#project_title: "Fifty shades of health research"
#lay_title: "AWARE: A tool to detect Avoidable WAste in health REsearch"
bibliography: .bib
body: "../source/body.md"
#summary: "../summary_of_proposal.md"
#lay_abstract: "../lay_abstract.md"
#sgba: "../sgba.md"
#output: html_document
#output:
#  word_document:
    #md_extensions: -autolink_bare_uris # disable auto recognition of links in Markdown
#    reference_docx: "template_cgh_newsletter_feature.docx"
#csl: csl/apa.csl
# The below did not work in older pandoc versions! They do work with 3.6.2
link-bibliography: true # If true, DOIs, PMCIDs, PMID, and URLs in bibliographies will be rendered as hyperlinks. (If an entry contains a DOI, PMCID, PMID, or URL, but none of these fields are rendered by the style, then the title, or in the absence of a title the whole entry, will be hyperlinked.) Defaults to true.
link-citations: true # If true, citations will be hyperlinked to the corresponding bibliography entries (for author-date and numerical styles only). Defaults to false.
output:
  md_document:
    variant: markdown_mmd  
    pandoc_args: [
      "--wrap=preserve",
      "--strip-comments=true"
    ]
csl: csl/apa.csl
#suppress-bibliography: false
---

```{r, include=FALSE}
# Calculate wordcount using rmdwc and stores in wc$words variable
# include=FALSE disables showing the snippet in output
#re <- "(```\\{r.+?\n```|`r .+?`|<!---.+?--->|^---\n.+?\n---)"
#wc <- rmdwc::rmdcount(rmarkdown::metadata$body, exclude=re) # use wc$words
# Calculate char count for proposal summary
#summary <- readLines(rmarkdown::metadata$summary, warn = FALSE)
#summary <- paste(summary, collapse = "\n")
#chars <- nchar(gsub(re, "", summary, perl = TRUE))
# Assign other useful variables
#title <- rmarkdown::metadata$project_title
#lay_title <- rmarkdown::metadata$lay_title
#heading <- '<div custom-style="Title">'
#textbox <- '<div custom-style="Textbox">'
#section <- '<div custom-style="Intense Quote">'
#red <- '<span custom-style="Red Text">'
#highlight <- '<span custom-style="Highlighted Text">'
#span <- '</span>'
#close <- '</div>'
```
